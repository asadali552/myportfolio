NODE_ENV=development
MONGO_URI=mongodb://127.0.0.1:27017/portfolioDB
ADMIN_EMAIL=connect.asadali8@gmail.com
# Prefer a bcrypt hash in production. Generate one with:
# node -e "console.log(require('bcryptjs').hashSync('replace-me', 12))"
ADMIN_PASSWORD_HASH=
JWT_SECRET=replace-with-at-least-32-random-characters
ALLOWED_ORIGINS=http://localhost:5500,https://asadali-dev.vercel.app
